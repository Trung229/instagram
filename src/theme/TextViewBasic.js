import React from 'react';
import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    textView:{
        fontSize:15,
        marginVertical:20,
        flex:1,
    },
    Delete:{
        color:"red"
    }
})
export default styles;