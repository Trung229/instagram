import React from 'react';
import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    inputField: {
        borderBottomWidth: 2,
        borderBottomColor:'#d2d2d2',
        padding:10,
        color: '#201d8d',
        fontWeight:'bold',
    },
    mrTop:{
        marginTop:60,
    }
})
export default styles;