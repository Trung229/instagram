import React from 'react';
import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    Line:{
        width:"100%",
        borderWidth:0.5,
        opacity:.3,
    }
    
})
export default styles;